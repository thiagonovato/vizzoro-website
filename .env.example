# Copy to .env.local for local dev. In production, set this under
# Vercel -> Project Settings -> Environment Variables instead.

# Resend (https://resend.com), used by src/app/api/contact/route.ts to
# email contact-form submissions to info@vizzoro.com.
# Same account/key already used for vncompliance.com and the vizzoro.com
# production site (git@github.com:thiagonovato/vizzoro-website.git).
RESEND_API_KEY=
